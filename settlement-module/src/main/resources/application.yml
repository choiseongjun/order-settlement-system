spring:
  application:
    name: settlement-service

  data:
    redis:
      host: localhost
      port: 6379
      password: redis_password
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 2

  datasource:
    url: jdbc:postgresql://localhost:5434/settlement_db
    username: settlement_user
    password: settlement_password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  kafka:
    bootstrap-servers: localhost:9093
    consumer:
      group-id: settlement-service
      auto-offset-reset: earliest
      enable-auto-commit: false
    producer:
      acks: all
      retries: 3
      enable-idempotence: true

logging:
  level:
    root: INFO
    com.example.settlement: DEBUG

server:
  port: 8083
